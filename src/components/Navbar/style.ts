import styled from 'styled-components';

export const NavItem = styled.div`
    display: flex;
    color: inherit;
    white-space: nowrap;
    flex-shrink: 0;
    text-decoration: none;
    padding: 0 10px;
`;
