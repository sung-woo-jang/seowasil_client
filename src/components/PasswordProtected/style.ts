import styled from 'styled-components';
import Colors from '../../styles/Colors';

export const PasswordProtectedWrapper = styled.div`
    width: 100%;
    height: 45vh;
    background: ${Colors.Gray7};
    display: flex;
    justify-content: center;
    align-items: center;
`;
